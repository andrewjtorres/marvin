parameters:
  searchFolder: reports/junit
steps:
  - task: PublishTestResults@2
    displayName: Publish tests results
    condition: succeededOrFailed()
    inputs:
      testResultsFiles: '**/*-results.xml'
      searchFolder: $(System.DefaultWorkingDirectory)/${{ parameters.searchFolder }}
